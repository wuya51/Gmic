/// <reference types="react" />
import { ConfirmationModalProps } from './ConfirmationModal.types';
export declare const ConfirmationModal: ({ modalId, closeModal, show, zIndex, handleOnModalUnmount, children, }: ConfirmationModalProps) => JSX.Element;
