import { Logger } from '@dynamic-labs/logger';
import { AuthMode } from '@dynamic-labs-wallet/core';
export declare const logger: Logger;
declare const setLoggerContext: ({ environmentId, authMode, sessionId, userId, }: {
    environmentId: string;
    authMode: AuthMode;
    sessionId?: string;
    userId?: string;
}) => void;
declare const logError: ({ message, error, context, }: {
    message: string;
    error: Error;
    context: Record<string, unknown>;
}) => void;
export { Logger, setLoggerContext, logError };
//# sourceMappingURL=logger.d.ts.map