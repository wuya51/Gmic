'use client'
'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
var client = require('@dynamic-labs-sdk/client');

/**
 * Sign in with passkey
 *
 * @returns Function to sign in with a passkey, returns a VerifyResponse
 *
 * @example
 * ```tsx
 * const App = () => {
 *   const signInWithPasskey = useSignInWithPasskey();
 *
 *   return (
 *     <button
 *       onClick={() => signInWithPasskey()}
 *     >
 *       Sign in with passkey
 *     </button>
 *  );
 * }
 */
const useSignInWithPasskey = ({ relatedOriginRpId, } = {}) => React.useCallback(() => client.signInWithPasskey({ relatedOriginRpId }), [relatedOriginRpId]);

exports.useSignInWithPasskey = useSignInWithPasskey;
