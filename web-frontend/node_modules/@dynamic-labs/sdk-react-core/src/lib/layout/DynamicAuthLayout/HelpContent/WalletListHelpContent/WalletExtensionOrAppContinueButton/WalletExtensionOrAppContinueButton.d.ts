/// <reference types="react" />
import { WalletOption } from '../../../../../shared';
type Props = {
    selectedUrl: string;
    wallet: WalletOption | undefined;
};
export declare const WalletExtensionOrAppContinueButton: ({ wallet, selectedUrl, }: Props) => JSX.Element;
export {};
