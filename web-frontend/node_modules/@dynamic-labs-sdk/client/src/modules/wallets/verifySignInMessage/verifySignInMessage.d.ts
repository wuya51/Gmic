import type { VerifyResponse, WalletProviderEnum } from '@dynamic-labs/sdk-api-core';
import type { DynamicClient } from '../../../client/types';
import type { Chain } from '../../chain';
import type { WalletAddressWithType } from '../walletAccount/walletAccount.types';
type VerifySignInMessageParams = {
    addressesWithTypes?: WalletAddressWithType[];
    chain: Chain;
    messageToSign: string;
    signature: string;
    walletAddress: string;
    walletDisplayName: string;
    walletProviderType: WalletProviderEnum;
};
/**
 * Verifies a sign in message by calling either the verifyLink or verify API function,
 * depending on whether the user is signing in or updating their account.
 *
 * Does NOT call updateAuthFromVerifyResponse, it should be called from the return of this function.
 */
export declare const verifySignInMessage: ({ messageToSign, walletAddress, addressesWithTypes, signature, chain, walletDisplayName, walletProviderType, }: VerifySignInMessageParams, client: DynamicClient) => Promise<VerifyResponse>;
export {};
//# sourceMappingURL=verifySignInMessage.d.ts.map