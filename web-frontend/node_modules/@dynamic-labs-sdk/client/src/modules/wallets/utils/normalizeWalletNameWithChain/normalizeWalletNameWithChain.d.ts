import type { Chain } from '../../../chain';
type NormalizeWalletNameWithChainParams = {
    chain: Chain;
    displayName: string;
};
/**
 * Format the raw wallet name and chain to get the value we can use for
 * verified credentials' `walletName` field.
 */
export declare const normalizeWalletNameWithChain: ({ displayName, chain, }: NormalizeWalletNameWithChainParams) => string;
export {};
//# sourceMappingURL=normalizeWalletNameWithChain.d.ts.map