import { ReactNode } from 'react';
import { WalletOption } from '../../../../../shared';
export declare const NewToWeb3WalletItem: ({ children, style, wallet, }: {
    children?: ReactNode;
    style?: Record<string, unknown>;
    wallet: WalletOption;
}) => JSX.Element;
