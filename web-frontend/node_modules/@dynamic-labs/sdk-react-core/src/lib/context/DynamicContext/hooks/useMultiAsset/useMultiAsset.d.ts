export type UseMultiAssetProps = {
    multiAssetOverride?: boolean;
    multiAssetSettings?: boolean;
};
export declare const useMultiAsset: ({ multiAssetOverride, multiAssetSettings, }: UseMultiAssetProps) => boolean;
