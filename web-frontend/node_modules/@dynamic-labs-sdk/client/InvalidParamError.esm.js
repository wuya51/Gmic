import { B as BaseError } from './constants.esm.js';

class InvalidParamError extends BaseError {
    constructor(message){
        super({
            cause: null,
            code: 'invalid_param_error',
            docsUrl: null,
            name: 'InvalidParamError',
            shortMessage: message
        });
    }
}

export { InvalidParamError as I };
